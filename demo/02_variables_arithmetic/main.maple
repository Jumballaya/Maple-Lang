export fn _start(x: i32): i32 {
  let a: i32 = x;
  let b: i32 = 12;
  let c: f32 = 3.5;
  let flag: bool = true;

  a = a + b;
  a = a * 2;
  a = a - 4;
  a = a / 2;
  c = c + 1.5;

  if (flag) {
    a = a + 1;
  }

  return a;
}
